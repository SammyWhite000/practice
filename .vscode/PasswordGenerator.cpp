#include <iostream>
#include <iomanip>
#include <vector>
#include <string>

using namespace std;

int main(){
    //fill different list with special characters
    string letterList[26] = {a, b, c, d, e, f, g, h, i, j};
    return 0;
}